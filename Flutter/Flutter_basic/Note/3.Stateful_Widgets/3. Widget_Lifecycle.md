### Widget LifeCycle
1. initState()
   - build()이전에 호출되며 한번만 불린다
   - 데이터 초기화 시 단순히 프로퍼티 정의하고 값을 할당해도 되지만 부모 요소에 의존하는 데이터를 초기화해야 하는 경우 initState() 활용 가능
2. build()
   - 위젯을 화면에 그려 줌
3. dispose()
   - 위젯이 제거되기 전에 호출 됨
   - dispose()안에서 이벤트 리스너 등을 취소하면 됨

