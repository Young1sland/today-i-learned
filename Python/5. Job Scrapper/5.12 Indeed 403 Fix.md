5.11챕터에서 indeed.com에 접근하면 403Error가 발생한다.  
이는 봇인지 체크하기 때문임. Selinium으로 브라우저인 것처럼 접근할 수 있다.  

#### Selinium을 설치하자
1. show hidden files
2. Config files -> replit.nix
3. { pkgs }: {
  deps = [
    pkgs.python38Full
    pkgs.chromium 추가
    pkgs.chromedriver 추가
  ];
  ...
}
4. import selenium
```py
from requests import get
from bs4 import BeautifulSoup
from extractors.wwr import extract_wwr_jobs
# selenium 사용 위한 import
from selenium import webdriver
from selenium.webdriver.chrome.options import Options

# 아래 세개의 옵션 replit에서 실행하기 위함이다
options = Options()
options.add_argument("--no-sandbox")
options.add_argument("--disable-dev-shm-usage")

base_url = "https://kr.indeed.com/jobs?q="
search_term = "python"

browser = webdriver.Chrome(options=options)
browser.get(f"{base_url}{search_term}&limit=50")

print(browser.page_source)

```