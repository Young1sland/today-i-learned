```py
#Set 자료형. 중복 허용하지 않고 순서 없음
websites = {"google.com", "airbnb.com", "twitter.com", "faebook.com","ticktok.com"}

#for문
for website in websites:
  print(website)

# Set은 순서가 없으므로 요소에 접근하려면 iter(), next()사용
print(next(iter(websites))) #google.com

```