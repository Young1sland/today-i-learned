Tuple은 주로 변경되지 않은 데이터를 그룹화하는데 사용. 불변성과 순서가 중요한 경우 사용. List보다 메모리 사용량 적고 빠르다.
```py
# Tuple
# 리스트와 달리 내용을 변경할 수 없음
days = ("Mon", "Tue", "Wed")
print(days[0])  #Mon

# 뒤에서부터 idx접근, List도 됨
print(days[-1])  #Wed
```
