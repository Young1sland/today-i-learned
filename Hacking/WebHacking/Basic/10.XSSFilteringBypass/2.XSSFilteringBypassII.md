## 불충분한 XSS 필터링

### 자바스크립트 함수 및 키워드 필터링
- 자바 스크립트는 Unicode escape sequence를 지원
```js
var foo = "\u0063ookie"; //cookie
var bar = "cooki\x65"; //cookie
\u0061lert(document.cookie); //alert(document.cookie)
```
- Computed member access
```js
document["coo"+"kie"] == document["cookie"]
```
