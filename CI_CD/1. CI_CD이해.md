## Cloud Native Application 특징
 - Microservices, DevOps, Containers, CI/CD

## CI/CD
- CI(Continuous Integration) : 빌드->테스트->패키징, ex) Jenkins, Travis CI, circleci
- CD(Continuous Delivery) : 수동 배포
- CD(Continuous Deployment) : 전 과정을 자동 배포

## Jenkins
- 지속적인 통합과 배포 -> Work flow 제어
- Build -> Test -> Deploy
- 다양한 Plugin 연동
  - Build Plugins : Maven, Gradle..
  - VCS(SCM,형상관리) Plugins : Git, SVN,..
  - Languages Plugins : java, python, NodeJS
### Jenkins 설치
https://www.jenkins.io/download/
https://github.com/jenkinsci/docker
```shell
 $ docker pull jenkins/jenkins:lts-jdk11
```

### docker jenkins 실행
```shell
# -p : 외부와 내부 포트 연결 (8080:8080은 8080 포트로 접근하면 도커 내부 8080으로 연결)
# -v : volume 마운트
$ docker run -d -v jenkins_home:/var/jenkins_home -p 8080:8080 -p 50000:50000 --restart=on-failure --name jenkins-server jenkins/jenkins:lts-jdk11
$ docker ps #docker 실행 확인
$ docker logs {Container Id or name} 
# 로그에서 초기 password 확인
```

### jenkins 접속
- http://localhost:8080
- 패키지 설치 및 계정 생성
- Global Tool Configuration -> JDK 설정(근데 실행되는 상태면 JDK가 설치되어 있다는 거니까 skip..)

### Item(Project) 생성
간단한 문장을 출력하는 project를 만들어보자.
- 새로운 아이템 -> Free Style -> Build Steps -> Execute shell    
    echo "Welcome to my first project using jenkins"
- 지금 빌드 -> 좌측 하단 -> Console Output으로 체크 (workspace 확인)
    Building in workspace /var/jenkins_home/workspace/My-First-Project ...
### Jenkins docker shell 접속
```shell
$ docker exec -it jenkins-server bash #bash shell 사용
$ cd /var/jenkins_home/workspace/ #workspace로 이동
```
