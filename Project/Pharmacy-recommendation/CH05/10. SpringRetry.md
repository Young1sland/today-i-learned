### Spring Retry
- https://wonyong-jang.github.io/spring/2021/02/18/Spring-Retry.html
- Retryable
  ```
   @Retryable(
    value = {RuntimeException.class} exception 선택
    maxAttempts = 2 재시도 횟수(최초 시도 포함)
    backoff = @Backoff(delay = 3000) 지연 시간
   )
  ```
- @Recover : Fallback 처리
```java


```