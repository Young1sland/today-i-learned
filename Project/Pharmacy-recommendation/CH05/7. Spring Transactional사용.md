### Transactional
- 스프링에서 트랜잭션 처리를 @Transactional이용해 처리
- @Transactional은 스프링 AOP 기반이며, 스프링 AOP는 Proxy 기반으로 동작
- @Transactional이 포함된 메소드가 호출될 경우, 프록시 객체를 생성함으로써 트랜잭션 생성 및 커밋 또는 롤백 후 트랜잭션 닫는 부수적인 작업을 프록시 객체에게 위임
- 지정된 메소드가 호출될 때 이 메소드를 가로채어 부가 기능들을 프록시 객체에게 위임
- 개발자가 메소드에 @Transactional만 선언하고 비지니스 로직에 집중 가능
- https://tedblob.com/spring-aop-proxy

### Self Invocation 문제
- 스프링 AOP 기반으로 하는 기능들(@Transactional, @Cacheable, @Async) 사용 시 Self Invocation 문제로 인해 장애가 발생할 수 있음
- 메소드가 호출되는 시점에 프록시 객체를 생성하고 프록시 객체는 부가 기능(Transaction)을 주입해 줌
- 문제 발생 예
```java
//@Transactional이 foo()에만 있고 bar에서 foo()를 호출
public class PharmacyRepositoryService {
    private final PharmacyRepository pharmacyRepository;

    public void bar(List<Pharmacy> pharmacyList){
        log.info("bar CurrentTransactionName : " + TransactionSynchronizationManager.getCurrentTransactionName());
        foo(pharmacyList);
    }

    @Transactional
    public void foo(List<Pharmacy> pharmacyList){
        log.info("foo currentTransactionName:" + TransactionSynchronizationManager.getCurrentTransactionName());
        pharmacyList.forEach(
                pharmacy -> {
                    pharmacyRepository.save(pharmacy);
                    throw new RuntimeException("error");
        });
    }
}
```
```groovy
def "self invocation"() {
        given:
        String address = "서울 특별시 성북구 종암동"
        String name = "은혜 약국"        
        def pharmacy = Pharmacy.builder()
                .pharmacyAddress(address)
                .pharmacyName(name)               
                .build()
        when:
        pharmacyRepositoryService.bar(Arrays.asList(pharmacy))

        then:
        //thrown : Exception이 발생하는지 체크
        def e = thrown(RuntimeException.class)
        println e.message
        //에러 발생으로 롤백될 것으로 기대..
        def result = pharmacyRepository.findAll()
        result.size() ==1 //트랜잭션이 적용되지 않는다(롤백 적용X)
    }
```
- 수정안    
  1.  트랜잭션 위치를 외부에서 호출하는 bar() 메소드로 이동
  ```java
  @Transactional
  public void bar(List<Pharmacy> pharmacyList){
    foo(pharmacyList);
  }
  public void foo(List<Pharmacy> pharmacyList){
     pharmacyList.forEach(
                pharmacy -> {
                    pharmacyRepository.save(pharmacy);
                    throw new RuntimeException("error");
        });
  }
  ```
  2. 객체의 책임을 최대한 분리하여 외부 호출 하도록 리팩토링
   ```java
   public class PharmacyRepositoryService {
    private final pharmacyService pharmacyService;
    public void bar(List<Pharmacy> pharmacyList){
        pharmacySerive.foo(pharmacyList);
    }
   ```

### 트랜잭션 읽기 전용 설정
- @Transactional(readOnly = true) 
- 스냅샷 저장 및 Dirty Checking 작업을 수행하지 않기 때문에 성능적으로 이점

### Transaction 우선 순위
- 클래스 보다 메소드가 우선 순위가 높음
- SimpleJpaRepository 살펴보면 클래스에 @Transactional(readOnly=true) 붙어 있고 변경 메소드에만 @Transactional이 따로 붙어 있음(readOnly=false)







