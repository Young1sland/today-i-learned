### 초기 Data Setup
1. sql 생성해서 저장하는 방법
   - 도커 컨테이너 생성 시 초기 데이터 생성
   - /docker-entrypoint-initdb.d/에 .sql 또는 .sh 파일 넣어두면 컨테이너 실행 시 실행 됨
   ```yml
   # docker-compose.yml
   # database/init폴더에 sql파일 넣어둠.
   pharmacy-recommendation-database:
    #...    
    volumes:
      - ./database/config:/etc/mysql/conf.d
      - ./database/init:/docker-entrypoint-initdb.d

   # application.yml
   jpa.hibernate.ddl-auto:validate
   ```
   - docker-compose up 후 DB 접속하여 확인
   ```shell
   $ docker-compose -f docker-compose-local.yml up
   $ docker exec -it {containerId} bash
   $ mysql -uroot -p
   > show databases;
   > use pharmacy-recommendation;
   > show tables;
   > select * from pharmacy;
   ```
   
2. csv파일 로드하여 저장하는 방법(github 참고해서 시도해보자)
   https://github.com/WonYong-Jang/Pharmacy-Recommendation
