### Auto Scaling Group
트래픽의 변동에 따라 자동으로 스케일 인(증설), 스케일 아웃(수축)하도록 설정할 수 있음

### 일래스틱 빈스톡 & IaC
- 일래스틱 빈스톡을 통해 자동으로 환경 구축 가능. 내부적으로 AWS Cloudformation 서비스 사용
- Clouldformation은 JSON, YML 작성을 통해 환경을 생성 업데이트 가능
- IaC(Infrastructure as Code) : 파일에 코드로 인프라를 묘사하고 서비스가 이를 반영해 인프라를 구축

