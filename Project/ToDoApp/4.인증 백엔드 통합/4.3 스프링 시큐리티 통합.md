# 스프링 시큐리트 통합

### Jwt TokenProvider 만들기
```java
@Slf4j
@Service
public class TokenProvider {
    private static final String SECRET_KEY = "qFlRpX30M";

    public  String create(UserEntity userEntity){
        Date expiryDate = Date.from(Instant.now().plus(1, ChronoUnit.DAYS));
        return Jwts.builder()
                .signWith(SignatureAlgorithm.HS512, SECRET_KEY)
                .setSubject(userEntity.getId())
                .setIssuer("demo app")
                .setIssuedAt(new Date())
                .setExpiration(expiryDate)
                .compact();
    }

    public String validateAndGetUserId(String token){
        //parseClaimsJws 메소드가 Base64 디코딩 및 파싱
        //위조됬다면 예외 날림
        //userId저장된 sub가 필요하므로 getBody()로 페이로드(Clams) 가져옴
        Claims claims = Jwts.parser()
                .setSigningKey(SECRET_KEY)
                .parseClaimsJws(token)
                .getBody();
        return claims.getSubject(); //sub 리턴
    }
}
```

### 스프링 시큐리티와 서블릿 필터
- 스프링 시큐리티는 서블릿 필터의 집합
- 서블릿 필터는 서블릿 실행 전에 실행되는 클래스들. 서블릿 필터는 디스패쳐 서블릿이 실행되기 전 항상 실행 됨.
- 서블릿 필터는 HttpFilter Or Filter를 상속하는 클래스
```java
//ServletFilter 예제
public class ExampleServletFilter extends HttpFilter {
    private TokenProvider tokenProvider;

    @Override
    protected void doFilter(HttpServletRequest request,
      HttpServletResponse response,
      FilterChain filterChain) throws IOException, ServletException {
        try{
            final String token = parseBearerToken(request); //bearer parsing 메소드
            if(token != null && !token.equalsIgnoreCase("null")){
                //validateAndGetUserId jwt에서 id가져오도록 만든 메소드
                String userId = tokenProvider.validateAndGetUserId(token); 
                filterChain.doFilter(request, response); //인증되면 다음 필터 실행
            }
        } catch(Exception e){
            response.setStatus(HttpServletResponse.SC_FORBIDDEN);
        }
    }

    private String parseBearerToken(HttpServletRequest request){
        String bearerToken = request.getHeader("Authorization");
        //StringUtils.hasText() 공백이나 null이면 false
        if(StringUtils.hasText(bearerToken) && bearerToken.startsWith("Bearer ")){
            return bearerToken.substring(7);
        }
        return null;
    }   
}
```

스프링 부트 사용하지 않는 경우 web.xml에 서블릿 컨테이너가 filter를 사용하도록 설정
```xml
<filter>
    <filter-name>ExampleServletFilter</filter-name>
    <filter-class>com.example.security.ExampleServletFilter</filter-class>
    /* 다른 매개변수들 */
</fitler>
<filter-mapping>
    <filter-name>ExampleServletFilter</filter-name>
    <url-pattern>/todo</url-pattern>
</filter-mapping>    
```

- 스프링 시큐리티 프로젝트를 추가하면 스프링 시큐리티가 FilterChainProxy라는 필터를 서블릿 필터에 끼워 넣어줌.
- FilterChaninProxy 클래스 안에서 내부적으로 스프링 빈 필터를 실행.
- 스프링이 빈 필터는 OncePerRequestFilter 필터 상속. 
- WebSecurityConfigurerAdapter 상속해서 필터 설정
  

### JWT를 이용한 인증 구현
